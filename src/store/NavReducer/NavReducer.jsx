import {createSlince} from "@reduxjs/toolkit"

const initialState = {

}

const NavSlice = createSlince({
    name:'nav',
    initialState,
    reducer:{

    }
})

export {}
export default NavSlice.reducer